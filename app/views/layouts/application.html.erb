<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf-8">
    <title><%= content_for?(:title) ? yield(:title) : "Bvvvvvbq" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
<div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <%= link_to "BBQ!", root_path, class: 'navbar-brand' %>

        <ul class="nav navbar-nav">
          <% if user_signed_in? %>
            <li><%= link_to 'Профиль', current_user %></li>
            <li><%= link_to 'Выйти', destroy_user_session_path,
                    method: :delete %></li>
          <% else %>
            <li><%= link_to 'Войти', new_user_session_path %></li>
            <li><%= link_to 'Зарегистрироваться',
                    new_user_registration_path %></li>
          <% end %>
        </ul>
</div>
</div>

    <div class="container">
      <div class="row">
        <div class="col-lg-9">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
        <div class="col-lg-3">
        </div><!--/span-->
      </div><!--/row-->

      <footer>
        <p>&copy; <%= Time.now.year %></p>
      </footer>

    </div> <!-- /container -->
  </body>
</html>
